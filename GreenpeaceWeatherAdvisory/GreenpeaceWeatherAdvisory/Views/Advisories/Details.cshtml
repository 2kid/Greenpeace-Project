@model IEnumerable<GreenpeaceWeatherAdvisory.Models.RecipientViewModel>


@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Advisory</h4>
    <hr />

    <p>@ViewBag.Advisory</p>

    <table class="table">
        <tr>
            <th>
                Contact Number
            </th>
            <th>
                Status
            </th>
        </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.ContactNumber</td>
                    <td>@item.Status</td>
                </tr>
            }
    </table>

</div>
